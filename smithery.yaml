# Smithery configuration: https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    (config) => ({ command: 'uvx', args: ['--from', 'xcpng-aiops', 'xcpng-aiops-mcp'], env: {} })
